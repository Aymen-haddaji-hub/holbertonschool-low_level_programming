#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void);
{
	char b;
	int i;

	while (i++ <= 9)
	{
		for (b = 'a'; b <= 'z'; b++)
			_putchar(b);
			_putchar("\n");
	return (0);
	}
}
